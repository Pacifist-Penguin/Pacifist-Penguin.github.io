type PostPreview = {
    description: string,
    img?: string,
    path: string,
    slug: string,
    title: string,
    extension: string,
    long: boolean,
    createdAt: string
}

export default PostPreview
